# 바이브 코딩 유의사항

바이브 코딩(흐름에 맞춰 코딩하기)할 때 참고할 체크리스트와 주의점입니다.

---

## 🎯 시작 전

- [ ] **목표를 한 줄로 정리**해 두기 — “지금 뭘 만들/고치려는지”가 흐려지면 방향이 흔들림
- [ ] **커밋 기준점** 잡기 — 큰 변경 전에 `git status` 확인, 필요하면 브랜치/커밋으로 되돌릴 수 있게
- [ ] **테스트/실행 방법** 알아두기 — 수정 후 “어떻게 확인할지”를 미리 정해 두면 안정적

---

## ⚡ 코딩 중

- **한 번에 하나씩** — 기능/버그 하나씩 끝내고 다음으로 넘어가기
- **이름 짓기** — 변수·함수·파일명을 직관적으로; 나중에 “이게 뭐였지?” 덜 나오게
- **주석은 “왜” 위주** — “무엇을” 하는 코드인지는 이름으로, “왜 이렇게 했는지”만 짧게.
    **CSS 코드는 클래스별로 주석 요청하고, JS도 함수별로 주석 요청**
- **자주 실행/테스트** — 한꺼번에 많이 바꾼 뒤에만 돌리면 원인 찾기 어려움
- **복붙/생성 코드 검토** — AI나 예제에서 가져온 코드는 흐름·스타일·보안 한 번씩 훑기

---

- **로컬에서 테스트할 수 있도록 임시 데이터를 이용하기** - API를 이용하는 기능은 로컬 환경에서 테스트해보기가 어려움. 일단 기능 작동되는 거 확인했으면, 디자인을 확인하기 위해서 임시 이미지를 사용하라고 하기. 그리고 아이콘은 SVG로 삽입.

---

## 🎵 Jukebox

- **재로딩 시 첫 번째 요소에 포커스** — Jukebox가 재로딩되는 모든 순간에, 목록의 첫 번째 요소(카드)가 **가운데(중앙)**에 오도록 한다. DOM focus가 아니라 Cover Flow에서 중앙에 배치되는 카드(jukebox-card--centered)를 말함. `fillJukeboxGallery`에서 `scrollLeft=0`으로 스크롤 초기화하면 좌측 스페이서 덕분에 첫 카드가 중앙에 옴.

---

## 🔘 Button 컴포넌트 (`Button.js`)

- **variant만 사용** — `renderButton({ variant, ariaLabel, content, ... })` 호출 시 `className`을 붙이지 않는다.
- **개별 스타일 재정의 금지** — 각 페이지·컴포넌트에서 버튼에 맞춰 스타일을 새로 만들거나 덮어쓰지 않는다.
- **컨텍스트별 스타일** — 필요하면 `Button.css`에 부모 선택자로 추가하거나, 해당 컴포넌트 CSS에서 `.부모 .btn--variant`처럼 부모 기준으로만 조정한다. 버튼 자체에는 임의의 class를 붙이지 않는다.

---

## 📝 변수명 규칙 (통일)

| 구분 | 규칙 | 예시 |
|------|------|------|
| 변수, 함수, 매개변수 | **camelCase** | `currentPage`, `getPageNumber()` |
| 상수 (변경 없음) | **UPPER_SNAKE_CASE** | `MAX_PAGES`, `API_URL` |
| 클래스, 컴포넌트명 | **PascalCase** | `PdfModal`, `FilterSubMenu` |
| CSS 클래스 | **kebab-case** | `pdf-nav-prev`, `btn--toolbar` |
| 파일/폴더명 | 컴포넌트는 PascalCase, 나머지는 kebab-case | `PdfModal.js`, `optimize-image-url.js` |

---

## 🧹 마무리할 때

- [ ] **실제로 실행해서** 한 번 더 확인
- [ ] **쓸데없는 코드/주석** 정리 (디버그용 `console.log`, TODO 등)
- [ ] **커밋 메시지** — “무엇을, 왜” 바꿨는지 간단히 적기
- [ ] **문서/README** — 새로 만든 기능·설정이 있으면 필요한 만큼만 업데이트

---

## 💡 바이브 유지 팁

- **피로하면 멈추기** — 실수는 보통 이때 늘어남
- **작은 성공** — “한 가지라도 동작하게” 만들고, 그다음 확장
- **되돌리기 두려워하지 않기** — 버전 관리 있으면 과감히 롤백 후 다시 시도

---

*이 파일은 프로젝트 루트의 `VIBE_CODING_NOTES.md`입니다. 팀/본인 방식에 맞게 수정해서 쓰면 됩니다.*
